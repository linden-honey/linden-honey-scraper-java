version: "3.7"
services:
  scraper:
    container_name: linden-honey-scraper
    image: lindenhoney/linden-honey-scraper:latest
    build: .
    ports:
      - 8080:8081
    networks:
      - linden-honey-net

networks:
  linden-honey-net:
    driver: bridge
